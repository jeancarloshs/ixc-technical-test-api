{
    "name": "api-chat-ixc",
    "version": 2,
    "public": true,
    "builds": [
      {
        "src": "src/**/*.ts",
        "use": "@vercel/node",
        "config": { 
            "includeFiles": ["dist/**"] 
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "dist/app.js"
      }
    ]
}
